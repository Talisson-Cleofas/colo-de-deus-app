import { Alert } from '@mui/material';import type { IntegrationStatus } from '../maps/types';export function DriveStatus({status}:{status:IntegrationStatus|null}){if(!status)return <Alert severity="info">Verificando Google Drive...</Alert>;return <Alert severity={status.enabled&&status.configured?'success':'info'}>{status.message||'Integração com Google Drive ainda não ativada.'}</Alert>}
