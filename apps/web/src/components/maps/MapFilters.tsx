import { MenuItem, TextField } from '@mui/material';export function MapFilters({value,onChange}:{value:string;onChange:(value:string)=>void}){return <TextField select size="small" label="Tipo" value={value} onChange={e=>onChange(e.target.value)}><MenuItem value="ALL">Todos</MenuItem><MenuItem value="MEMBER">Membros</MenuItem><MenuItem value="CELL">Células</MenuItem><MenuItem value="CENACLE">Cenáculos</MenuItem><MenuItem value="EVENT">Eventos</MenuItem></TextField>}
