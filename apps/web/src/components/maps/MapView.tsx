import { Box, Card, CardContent, Link, Typography } from '@mui/material';import type { MapMarkerData } from './types';
export function MapView({markers,height=420}:{markers:MapMarkerData[];height?:number}){return <Box sx={{minHeight:height,display:'grid',gridTemplateColumns:{xs:'1fr',md:'repeat(2,1fr)'},gap:2}}>{markers.length===0?<Typography color="text.secondary">Nenhuma localização válida encontrada.</Typography>:markers.map(marker=><Card key={marker.id} variant="outlined"><CardContent><Typography fontWeight={800}>{marker.title}</Typography><Typography variant="body2" color="text.secondary">{marker.address||`${marker.latitude}, ${marker.longitude}`}</Typography><Link href={marker.navigationUrl} target="_blank" rel="noreferrer">Abrir no Google Maps</Link></CardContent></Card>)}</Box>}
